# Contrato de Seguimiento de Monedas

Para el contrato de seguimiento de monedas, se requiere la siguiente estructura de respuesta:

```javascript
const response = [
  {...},
  {...},
  {...},
  //...
];

const obj = {
  "icono": "string",
  "nombre": "string",
  "siglas": "string",
  "ratio": float,
  "precio": float,
};
